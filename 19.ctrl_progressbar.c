/**
 * 
 * 在上例中，通过时间来控制进度条的显示
 * 在本例中，我们要实现能通过左右键来控制进度的的显示 
 * 
 * 要求：
 *  - 显示一个进度条，初始值为50%
 *  - 按左键，进度条减少1%，直至0%
 *  - 按右键，进度条增加1%，直至100%
 *  
 * 知识点：
 *  - 接收按键输入<左键>,<右键>，  scanf("%c",&ch);<左键>,<右键> ACCISS码   \r\n
 *  - 不进行换行，每次回到行头，回车符
 *  - 每次执行render_progressbar进行渲染进度条时，均回车到行头
 *  - 每次执行render_progressbar均要清空当前行屏幕
*/


void render_progressbar(int value){
    const int width = 80;
    const char  ch = "#";





    

}

int main(){

char leftKey=113,rightKey=223;
char key;

while (key="0")
{
    /* code */
}

scanf("%c",&key);

printf("123\b***"); 
printf("##############");

}
